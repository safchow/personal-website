services:
  mongodb:
    build:
      context: ./mongodb_replica
      args:
        MONGO_VERSION: 7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
